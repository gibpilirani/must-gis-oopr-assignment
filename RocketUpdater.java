public class RocketUpdater {
       public static void updateRocket(Vehicle vehicle, double newSpeed, double newTemperature, double newFuelLevel) {
        vehicle.setSpeed(newSpeed);
        vehicle.setTemperature(newTemperature);
        vehicle.setFuelLevel(newFuelLevel);


    }


}
